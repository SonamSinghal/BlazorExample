@page "/allEmployees"

<h3>AutoGeneratedTable</h3>

<GenericRenderFragment items="list"></GenericRenderFragment>

@code {

    private List<Employees> list { get; set; }

    public class Employees
    {

        public Employees()
            {

            }

        public Employees(int id, string name, string dept)
        {
            Id = id;
            Name = name;
            Department = dept;
        }
        public int Id { get; set; }
        public string Name { get; set; }
        public string Department { get; set; }
    }


    protected override void OnInitialized()
    {
        base.OnInitialized();

        Employees emp = new Employees();
        list = new List<Employees>()
        {
            new Employees(emp.Id=1, emp.Name="User "+emp.Id, emp.Department="CSE"),
            new Employees(emp.Id=2, emp.Name="User "+emp.Id, emp.Department="CSE"),
            new Employees(emp.Id=3, emp.Name="User "+emp.Id, emp.Department="CSE"),
            new Employees(emp.Id=4, emp.Name="User "+emp.Id, emp.Department="CSE"),
            new Employees(emp.Id=5, emp.Name="User "+emp.Id, emp.Department="CSE"),
            new Employees(emp.Id=6, emp.Name="User "+emp.Id, emp.Department="CSE"),
            new Employees(emp.Id=7, emp.Name="User "+emp.Id, emp.Department="CSE"),
            new Employees(emp.Id=8, emp.Name="User "+emp.Id, emp.Department="CSE"),
            new Employees(emp.Id=9, emp.Name="User "+emp.Id, emp.Department="CSE"),
            new Employees(emp.Id=10, emp.Name="User "+emp.Id, emp.Department="CSE"),
            new Employees(emp.Id=11, emp.Name="User "+emp.Id, emp.Department="CSE")
        };
    }

}